{
  "name": "nkgame",
  "version": "0.1.0",
  "private": true,
  "description": "Coop detective mystery game — 2 players, mobile PWA, WebRTC P2P with a 6-digit join code.",
  "type": "module",
  "scripts": {
    "start": "node server/signaling.js",
    "dev": "node --watch server/signaling.js",
    "generate-case": "node tools/generate-case.js"
  },
  "dependencies": {},
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "@google/genai": "^1.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.35.1"
  }
}
